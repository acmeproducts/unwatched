# DigitalOcean App Platform. One app, two services from images in the DigitalOcean container registry:
# the town (one long-lived process with WebSockets, reached under /town) and the web at /.
# deploy/do.sh builds, pushes, and applies this spec. Secrets are set once with `doctl apps update` and never committed.
name: small-hours
region: fra
# The island's own address. DNS points here with a CNAME to the app's default hostname; the certificate is DigitalOcean's.
domains:
  - domain: small-hours.world
    type: PRIMARY
  - domain: www.small-hours.world
    type: ALIAS
services:
  - name: town
    image:
      registry_type: DOCR
      repository: small-hours-town
      tag: latest
    http_port: 4000
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-1gb
    health_check:
      http_path: /api/health
      initial_delay_seconds: 20
    routes:
      - path: /engine
        preserve_path_prefix: false
    envs:
      - key: PORT
        value: "4000"
      - key: SH_BRAIN
        value: openrouter
      - key: SH_MS_PER_SIM_MINUTE
        value: "60000"
      - key: SH_SEED
        value: "42"
      - key: SH_CITIZENS
        value: "20"
      - key: SH_BOAT_SPACES
        value: "8"
      - key: SH_REAL_WORLD
        value: 43.17,16.44,Europe/Zagreb
      - key: SH_TOWN_NAME
        value: The island
  - name: web
    image:
      registry_type: DOCR
      repository: small-hours-web
      tag: latest
    http_port: 3000
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-0.5gb
    routes:
      - path: /
