{
  "name": "ferry-town",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "soak": "pnpm --filter @ferrytown/headless soak"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "turbo": "^2.10.0",
    "typescript": "^5.8.0",
    "vitest": "^5.0.0",
    "tsx": "^4.23.0"
  }
}
